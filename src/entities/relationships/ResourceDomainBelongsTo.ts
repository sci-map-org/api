// Naming: teaches / explains / references / belongs to / covers ?
// Should be the same as the resource -> concept one ?

export const ResourceDomainBelongsToLabel = 'BELONGS_TO';

export interface ResourceDomainTeaches {}
