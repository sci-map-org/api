// Naming: teaches / explains / references / belongs to
// Should be the same as the resource -> concept one

export const ResourceDomainTeachesLabel = 'TEACHES';

export interface ResourceDomainTeaches {}
