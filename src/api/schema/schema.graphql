type User {
  _id: String!
  email: String!
}

type CurrentUser {
  _id: String!
  email: String!
  # private stuff here
}

type LoginResponse {
  currentUser: CurrentUser!
  jwt: String!
}

type Query {
  currentUser: CurrentUser!
}

input SignupPayload {
  displayName: String!
  uniqueName: String! # For profile url, @...
  email: String!
  password: String!
}

type Mutation {
  login(email: String!, password: String!): LoginResponse!
  signup(payload: SignupPayload!): CurrentUser!
}
